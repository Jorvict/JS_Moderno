// Detectar cuando el HTML esta listo

console.log(1);

document.addEventListener('DOMContentLoaded', () => { // Este evento se ejecuta automaticamente una vez se ha descargado todo el HTML
    // console.log('Documento listo');
    console.log(2);
});

console.log(3);