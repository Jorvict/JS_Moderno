// El Orden de las operaciones
let resultado;

// Multiplicaciones y divisiones se ejecutan primero
// Sumas y restas se ejecutan después

resultado = 20 + 30 * 2; // Da 80, 30 * 2 = 60 + 20 = 80

resultado = (20 + 30) * 2; // Al tener parentesis, primero suma y luego multiplica

// 20% de descuento en tu carrito de compras
resultado = (20 + 30 + 40 + 50 + 60) * .2;

// Impuesto 16%
resultado = (20 + 30) * 1.16;

console.log(resultado);